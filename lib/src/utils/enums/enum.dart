enum PermissionType { camera, medialibrary, photo, storage }

enum SupportState {
  unknown,
  supported,
  unsupported,
}

enum ChatUserType { receiver, sender }

enum AppState {
  opened,
  resumed,
  paused,
  inactive,
  detached,
}

enum SubscriptionScreenType {
  isFromSetUpProfile,
  isFromChat,
  isFromBoost,
  isFromSwipe,
  isFromSubscription
}
